# Claude Code — personal / machine-local settings (never commit)
.claude/settings.local.json

# CodeGraph — local index DB, daemon, sockets, logs (machine-local)
.codegraph/codegraph.db
.codegraph/codegraph.db-shm
.codegraph/codegraph.db-wal
.codegraph/daemon.log
.codegraph/daemon.pid

# Gradle
.gradle/
build/

# H2 file-based datastore (generated at runtime)
data/

# IDE
.idea/
*.iml

# Docs, tasks & spec — kept local, never pushed
docs/
tasks/
SPEC.md

# Project code & build — kept local, never pushed
build.gradle
settings.gradle
